# Copy this file to .env to override default configuration.
# No secrets are required — SauceDemo's test accounts are public fixture data
# documented in test_data/test_data.json, not credentials that need protecting.

# Base URL of the application under test. Override to point the suite at a
# staging environment, a local mock, etc.
BASE_URL=https://www.saucedemo.com/
