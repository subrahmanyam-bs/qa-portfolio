# Screenshots

Empty on purpose. No browser automation tool was available in this environment, so no real UI screenshots of the Admin dashboard or storefront got taken. Rather than fake it, the affected test cases are marked **Blocked** in their own files (full list in `test-execution/execution-summary.md`).

Evidence for this project is instead the literal, reproducible HTTP request/response pairs in `test-cases/`, `api-testing/api-test-cases.md`, and `bug-reports/`.
